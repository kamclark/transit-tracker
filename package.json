{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "npx nuxi generate --preset github_pages",
    "deploy": "npm run generate && npx gh-pages -d dist"
  },
  "dependencies": {
    "@nuxt/eslint": "^1.4.1",
    "eslint": "^9.28.0",
    "nuxt": "^3.17.4",
    "vue": "^3.5.16",
    "vue-router": "^4.5.1"
  }
}